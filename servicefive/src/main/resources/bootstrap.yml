spring:
  cloud:
    config:
      label: master
      profile: dev
      discovery:
        service-id: serviceone
        enabled: true
    bus:
      refresh:
        enabled: true

  kafka:
    bootstrap-servers: 192.168.236.133:9092


eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8081/eureka/